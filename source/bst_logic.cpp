#include "bst_logic.h"

NodeBST* insertBST(NodeBST* root, Paket data) {
    // KODE AKAN DIKERJAKAN OLEH NABIL
    return root;
}

NodeBST* searchBST(NodeBST* root, string resi) {
    // KODE AKAN DIKERJAKAN OLEH NABIL
    return nullptr;
}